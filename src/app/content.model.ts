import { Collection } from './collection.model';

export class Content {
    constructor(public name: string, public description: string, public details: Object, public thumbnail: string, public primary: boolean){}
}
